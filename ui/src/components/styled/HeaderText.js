import styled from "styled-components"
import { SECONDARY_FONT_COLOR } from "../../helpers/styles"

const HeaderText = styled.h3`
  font-weight: 400;
  font-color: ${SECONDARY_FONT_COLOR};
`

export default HeaderText
